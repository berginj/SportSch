---
interface Props {
  siteName: string;
  region: string;
  email: string;
  phone?: string;
  sponsorshipEmail?: string;
  instagramUrl: string;
  facebookUrl: string;
  partnerOrgName?: string;
  partnerOrgUrl?: string;
}

const { siteName, region, email, phone, sponsorshipEmail, instagramUrl, facebookUrl, partnerOrgName, partnerOrgUrl } = Astro.props as Props;
const year = new Date().getFullYear();
---

<footer class="mt-16 border-t border-slate-200 bg-brand-secondary text-white">
  <div class="container-shell grid gap-8 py-12 md:grid-cols-3">
    <div>
      <h2 class="font-display text-2xl font-bold">{siteName}</h2>
      <p class="mt-3 text-sm text-slate-200">{region}</p>
      <p class="mt-2 text-sm">
        <a href={`mailto:${email}`} class="text-slate-100">{email}</a>
      </p>
      {phone && (
        <p class="mt-1 text-sm">
          <a href={`tel:${phone.replace(/[^\d]/g, "")}`} class="text-slate-100">{phone}</a>
        </p>
      )}
      {sponsorshipEmail && (
        <p class="mt-1 text-sm">
          Sponsorships:
          <a href={`mailto:${sponsorshipEmail}`} class="ml-1 text-slate-100">{sponsorshipEmail}</a>
        </p>
      )}
    </div>
    <div>
      <h3 class="text-sm font-bold uppercase tracking-wide text-slate-200">Site Links</h3>
      <ul class="mt-3 space-y-2 text-sm">
        <li><a href="/schedule">Schedule</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/policies">Policies</a></li>
        <li><a href="/contact">Contact</a></li>
      </ul>
    </div>
    <div>
      <h3 class="text-sm font-bold uppercase tracking-wide text-slate-200">Support AGSA</h3>
      <ul class="mt-3 space-y-2 text-sm">
        <li><a href="/get-involved">Get Involved</a></li>
        <li><a href="/programs">Programs</a></li>
        <li><a href="/sponsors">Sponsors</a></li>
        <li><a href="/sponsor">Sponsor Form</a></li>
      </ul>
      <div class="mt-3 flex gap-3 text-sm">
        <a class="btn-secondary !border-white/30 !bg-white/10 !text-white" href={instagramUrl} target="_blank" rel="noreferrer">Instagram</a>
      </div>
      <p class="mt-3 text-xs text-slate-300">
        Facebook (link only):
        <a class="ml-1 underline" href={facebookUrl} target="_blank" rel="noreferrer">AGSA Facebook page</a>
      </p>
    </div>
  </div>
  <div class="border-t border-white/15">
    <div class="container-shell py-4 text-xs text-slate-300 space-y-2">
      <p class="m-0">(c) {year} {siteName}. All rights reserved.</p>
      {partnerOrgName && partnerOrgUrl && (
        <p class="m-0">
          Looking for baseball? Visit our partner organization:
          <a class="ml-1 text-white underline" href={partnerOrgUrl} target="_blank" rel="noreferrer">{partnerOrgName}</a>
        </p>
      )}
    </div>
  </div>
</footer>
